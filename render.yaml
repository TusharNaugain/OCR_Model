services:
  - type: web
    name: ocr-backend
    env: docker
    region: oregon
    plan: free
    rootDir: ocr-web-app/backend
    dockerContext: ocr-web-app/backend
    envVars:
      - key: PORT
        value: 8000
    # Health check path to verify service is up
    healthCheckPath: /
